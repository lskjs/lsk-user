export default (ctx) => {
  return {
    init() {
      // this.components = require('./components').default(ctx);
      // this.models = require('./models').default(ctx);
      // this.router = require('./router').default;// (ctx);
    },
  };
};
